spring.application.name=mp2

spring.data.mongodb.host=localhost
spring.data.mongodb.port=27017
spring.data.mongodb.database=mp2

spring.datasource.username=${MYSQLUSERNAME}
spring.datasource.password=${MYSQLPASSWORD}
spring.datasource.url=jdbc:mysql://localhost:3306/mp2

# JWT Configuration
jwt.secret=${JWT_SECRET}
jwt.expiration-time=3600000

# Debugging
logging.level.org.springframework.security=DEBUG
# spring.devtools.restart.exclude=static/**,public/**,templates/**,resources/**
# firebase.config.path=your-config-file.json

# App
url.judge0=${URL_JUDGE0}
